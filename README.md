# CIBR-Aves
Para el funcionamiento de esta app es necesario tener instalado Python 3.6 o superior e instalar las siguientes dependencias:

-   pip install scikit-image
-   pip install Flask, flask-cors

Para inciar el proyecto, hacer uso del comando: python startCBIR.py